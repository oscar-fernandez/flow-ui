import { vi, describe, it, expect } from "vitest";
import { useState } from "react";
import { useCustomNavigate, useToggle } from "./customHooks";
import { renderHook } from "@testing-library/react";

describe("SidebarItems Hook test", () => {
  // it("useToggle should take in a boolean value and return that value plus the handleClick function",()=>{
  // })
});
